# obfus-link-demo
Runnable, zero-dependency example: turn a messy LLM response into clean, typed data in two deterministic calls against   the obfus.link MCP grid (llm_to_json_cleaner → json_to_zod). No API key — runs on the free tier. Node 18+.
